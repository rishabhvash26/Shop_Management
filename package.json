{
  "name": "shop-management-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "Shop Management prototype: Inventory, Sales Orders, Purchase Orders, Daily Transactions. Backend on Express + firebase-admin (Firestore Emulator only). Frontend on React + Vite.",
  "scripts": {
    "emulator": "firebase emulators:start --only firestore",
    "server": "npm run start --prefix server",
    "client": "npm run dev --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  },
  "overrides": {
    "universal-analytics": {
      "uuid": "9.0.1"
    }
  }
}
